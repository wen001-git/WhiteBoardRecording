# 白板录制工具 · 项目计划（PROJECT_PLAN）

> 单一产品文档：需求要点 + 功能清单 + 设计决策 + 测试清单 + 里程碑。
> 运行/状态/TODO 见仓库根 [`AGENTS.md`](../AGENTS.md)；使用说明见 [`README.md`](../README.md)。

## 一句话定位
仿 [excalicord.com](https://excalicord.com/) 的**纯前端单 HTML** 白板录制工具：无限白板画画/写字 + 角落摄像头 + 提词器 + 一键录制导出 mp4。**无后端、零依赖**。

## 需求要点
- 在无限白板上自由绘画/写字，可平移、缩放。
- 摄像头画面出现在角落并录进视频；麦克风录音。
- 内置提词器看稿，且**不被录进视频**。
- 一键录制（开始/暂停/继续/停止）+ 计时，导出 **mp4** 保存本地。
- 双击即用风格：单个 HTML 文件，方便保存/分享。
- 目标用户：老师 / 知识博主 / 远程异步沟通者。

## 功能清单
**白板/画布**：F-1 全屏画布 + 无限平移（hand 工具 / 空格拖拽）｜F-2 滚轮以光标为中心缩放 + 比例显示｜F-3 对象数组存储、重绘不丢。
**绘图工具**：F-4 画笔（pointer，支持鼠标/触屏/笔）｜F-5 颜色｜F-6 粗细（细/中/粗）｜F-7 矩形｜F-8 圆/椭圆｜F-9 箭头｜F-10 文字（点击输入、可设字号）｜F-11 橡皮擦（对象级命中删除）｜F-12 撤销/重做｜F-13 清空。
**摄像头/音频**：F-14 角落摄像头小窗｜F-15 可拖动换位与拖拽缩放｜F-16 麦克风｜F-17 录制设置中可选择麦克风设备、摄像头大小与圆形/方形。
**提词器**：F-18 可开关浮层｜F-19 编辑稿子｜F-20 字号｜F-21 自动滚动 + 调速/播停/回顶｜F-22 **不进录像**。
**录制/导出**：F-23 开始/暂停/继续/停止｜F-24 计时 + 录制中指示｜F-25 白板+摄像头合成为一个视频｜F-26 合并麦克风音轨｜F-27 导出 mp4（原生优先，否则 webm + ffmpeg.wasm 转码回退）｜F-28 停止后预览 + 下载。
**手绘风格（迭代二）**：F-31 矩形/圆/箭头为手绘外观，每个形状带稳定 `seed`、按 seed 确定性生成线条扰动（每帧重绘不闪、不同形状线条各异）；画布文字用手写字体栈。
**录屏模式（迭代四）**：F-36 两种录制来源——① 录白板(现状) ② 录屏幕：`getDisplayMedia` 选屏幕/窗口/标签页 → 在实时预览上用**取景框框选子区域**(锁比例)→ 裁剪该区域 + 角落摄像头(Loom 风) + 麦克风&系统/标签声音(WebAudio 混音)；Chrome/Edge 用 `MediaStreamTrackProcessor` 帧驱动合成,切到别的 App 后台仍满帧;Safari/Firefox 降级(无 `MediaStreamTrackProcessor`/系统音则回退)。
**录制比例与背景（迭代二/三）**：F-32 录制设置弹窗——画面比例预设（16:9 YouTube / 4:3 经典 / 3:4 小红书 / 9:16 抖音 / 1:1 / Custom 自定义）+ 背景壁纸（分类筛选、随机选择、离线渐变/纹理/纯色/无）；F-33 可拖动+四角缩放并锁定比例的取景框界定录制区域；F-34 输出 = 选定比例画布上，背景 + 居中白卡片（边距+圆角+阴影）承载取景框内容 + 摄像头按相对位置映射进卡片；F-35 录制设置支持白卡片圆角/边距、摄像头显示开关/大小/圆方形、麦克风选择、光标高亮开关/颜色。

## 关键设计决策
- **架构**：纯前端单 `index.html`，HTML/CSS/JS 全内联。仅当将来要账号/云存储/分享/服务端转码/协作时才加后端——届时本前端逻辑可复用。
- **安全上下文**：`getUserMedia` 需 https 或 localhost；`file://` 多被拦。运行用 `python3 -m http.server` 或部署到静态托管。
- **绘图模型**：`state.scene[]` 对象数组 + `view{x,y,scale}`，每帧重绘；`undoStack/redoStack` 用快照实现撤销/重做。坐标：世界 = (屏幕 − view.xy) / scale。
- **合成录制**：离屏 `recCanvas`（=视口像素）每帧 `drawImage(白板)` + 角落 `drawImage(摄像头)`（圆形裁剪+镜像，位置跟随小窗）→ `captureStream(30)` + 麦克风音轨 → `MediaRecorder`。**用 `setInterval(1000/30)` 驱动合成**（不用 rAF：页面切后台时 rAF 暂停会导致录制黑屏）。
- **mp4 导出**：`MediaRecorder.isTypeSupported('video/mp4;codecs=avc1...')` 为真则直接录 mp4；否则录 webm，提供「转 mp4」按钮**按需**从 CDN 懒加载 ffmpeg.wasm 转码（仅此一处联网）。
- **提词器隔离**：提词器是普通 DOM 浮层，不属于被 `drawImage` 的白板 canvas，天然不进录像。
- **手绘渲染（自写零依赖）**：`mulberry32(seed)` PRNG + `roughLine/roughRect/roughEllipse/roughArrow`；线条扰动幅度随线宽。形状对象存 `seed`，重绘按 seed 确定性生成→稳定不闪。不引 rough.js，保持单文件。
- **录制比例/背景/取景框**：`RATIOS` 决定输出像素与画布比例，`custom` 读取 `customW/customH`；`BACKGROUNDS` 提供离线渐变、纯色、程序纹理与无背景；`recConfig` 统一保存比例、背景、取景框、白卡片圆角/边距、摄像头、麦克风、光标高亮设置。录制状态机 idle→setup→recording→paused。`drawRecFrame` 每帧：填背景 → 画白卡片(边距+圆角+阴影) → 裁剪卡片绘制取景框区域的白板像素 → 摄像头按屏幕相对位置映射进卡片 → 可选光标高亮。取景框 `#recFrame` 拖动/缩放时锁定比例。

## 测试清单
本地起 `python3 -m http.server 8000` → 开 `http://localhost:8000` → 手测 + 截图，控制台监控报错。Chrome / Safari 各一轮。

| 编号 | 用例 | 预期 | 状态 |
|------|------|------|------|
| T-1 | 画笔换色换粗细 | 颜色粗细正确、平滑 | ✅ 已验(无头) |
| T-2 | 矩形/圆/箭头 | 图形与属性正确 | ✅ 已验(无头) |
| T-3 | 文字输入 | 落点/字号正确 | ✅ 已验(无头) |
| T-4 | 橡皮擦 + 撤销/重做 | 删除/恢复正确 | ✅ 已验(无头) |
| T-5 | 平移 + 缩放 | 内容不错位、比例正确 | ✅ 已验(无头) |
| T-6 | 授权摄像头、拖动小窗 | 角落出现画面、可换位 | ⏳ 待真机 |
| T-7 | 提词器粘文本/调字号/滚动/暂停 | 平滑可控 | ⏳ 待真机 |
| T-8 | 开始→暂停→继续→停止 | 状态/计时正确 | ⏳ 待真机 |
| T-9 | 录制中画图+说话 | 产物含白板+人脸+声音 | ⏳ 待真机 |
| T-10 | 回看录像看提词器 | 录像中**无提词器** | ⏳ 待真机 |
| T-11 | 导出并播放 | 文件为 mp4 且可播 | ⏳ 待真机 |
| T-12 | Chrome/Safari 各跑 T-9~T-11 | 均产出可播 mp4 | ⏳ 待真机 |
| T-13 | 画 ~200 对象后操作 | 流畅，录制约 30fps | ⏳ 待真机 |
| T-14 | 同一形状重复重绘/缩放 | 线条不闪、形状稳定（seed 确定性） | ✅ 已验(无头) |
| T-15 | 录制设置选不同比例 | recCanvas 尺寸=预设、取景框锁比例 | ✅ 已验(无头) |
| T-16 | 选渐变背景录制 | 输出含渐变背景+白卡片+取景框内容 | ✅ 已验(无头) |
| T-17 | 各比例(16:9/9:16/3:4…)真机录制 | 输出为对应比例、装饰正确、含人脸+声音 | ⏳ 待真机 |
| T-18 | 打开录制设置弹窗 | 6 个比例、背景分类/随机、圆角/边距、摄像头、麦克风、光标控件可见且无控制台错误 | ✅ 已验(本地浏览器) |
| T-19 | 录制内容切「屏幕」 | body.src-screen、隐藏背景/圆角边距/光标(board-only)、保留比例/摄像头/麦克风;函数与能力检测就绪、无报错 | ✅ 已验(无头) |
| T-20 | 录屏:选屏幕→取景框选区→开始→切App→停止 | 只录框内区域、切 App 满帧不冻结、脸在角落、麦克风+系统声、mp4 可播 | ⏳ 待真机(Chrome) |

> "无头"= 在 Chromium 无头预览中以脚本验证了逻辑/渲染；摄像头与真实录制产物需在可见的真实浏览器+授权下手测。

## 里程碑
- **M0~M3 完成**：单文件实现全部基础功能；无头预览验证逻辑/渲染/native mp4 支持、零报错。
- **迭代二完成（待真机）**：手绘风格形状 + 录制比例/背景/取景框；无头预览验证手绘确定性、录制设置逻辑、合成输出。
- **迭代三完成（待真机）**：绘图样式配置、摄像头头像可拖拽缩放、更细真实手绘线条、完整录制设置；已验证设置 UI 渲染和内联 JS 语法，真实摄像头/麦克风录制仍需真机授权验证。
- **待办**：真机跨浏览器手测（T-6/T-9~T-12/T-17）、UI 打磨。

## 变更日志

| 日期 | 变更内容 |
|------|---------|
| 2026-06-29 | 迭代四：新增「录屏幕」模式(getDisplayMedia + 取景框裁剪 + 角落摄像头 + 麦克风/系统声音混音;Chrome 帧驱动合成切后台满帧)。F-36、T-19/20 |
| 2026-06-29 | 迭代三：补齐完整录制设置（Custom、背景分类/随机、圆角/边距、摄像头、麦克风、光标高亮）并接入录制合成；更新测试项 |
| 2026-06-29 | 迭代三：修复录制设置「画布边距」预览不生效 bug；预览改为输出的等比缩小真实渲染(画框+按边距内缩白卡片)，对齐 excalicord |
| 2026-06-28 | 迭代二：新增手绘风格形状(F-31)、录制比例/背景/取景框(F-32~34)；补充 T-14~17 测试项 |
| 2026-06-28 | 将原 docs/01~06 六份文档合并为本单一项目文档；同步实现状态与测试结果 |
