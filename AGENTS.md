> 目的：让任意 AI 工具以最少上下文接手 WhiteBoard。　目标读者：接手开发的 AI 或开发者。　如何阅读：会话开始只读本文；确定任务后，只读 [`docs/IMPL_NOTES.md`](docs/IMPL_NOTES.md) 的对应小节。

# AGENTS.md — WhiteBoard 精简接手索引

> 本文是当前状态索引，不保存长篇实现历史。实现细节按需查 `docs/IMPL_NOTES.md`，产品规划按需查 `docs/PROJECT_PLAN.md`，历史使用 `git log --oneline --stat`。不要预读全部文档、全量扫描代码库或输出大型 HTML 的完整 diff。

## 一句话定位

单 HTML 白板录制工具：浏览器本地完成编辑、幻灯片、摄像头、录屏与导出；静态入口和轻量 Node + Neon 服务提供购买、账号与设备授权。

## 运行与最小测试

```bash
cd /Users/Zhuanz/Claude/WhiteBoard
npm install
npm run build:static                 # 校验公共发布白名单
python3 -m http.server 8000          # 静态页：http://localhost:8000
npm start                            # 账号 API；需先配置 .env
```

- 文档、文案、注释、纯样式及其他低风险局部修改不默认运行测试，只检查目标 diff 或做必要的静态校验。
- 只有复杂逻辑、跨模块改动，或涉及登录、数据保存、录制等关键路径时，才运行与本次修改直接相关的最小测试；发布前、影响范围难判断或用户明确要求时再升级为 `npm test`。
- 摄像头、麦克风和录屏必须使用 HTTPS 或 localhost，不能依赖 `file://`。

## 硬约束

1. 白板编辑器继续保持独立单 HTML，账号服务不得侵入绘图和录制核心。
2. 登录顺序必须是本地 `accounts.json` 优先、Neon `/api/login` 兜底；有效静态账号必须立即放行，不得等待后端。允许随后通过 `/api/login-audit` 非阻塞核验重叠 Neon 账号并记录 IP；修改登录时同步检查 `index.html` 与 `whiteboard-pro.html`。
3. 公共站点只能由 `npm run build:static` 的白名单产物发布；不得发布仓库根目录，不得提交数据库连接串、明文密码、Cookie 密钥或管理令牌。
4. 录制来源只有白板与屏幕两类，摄像头、麦克风和来源画面最终合成一个视频；提词器及选择框、幻灯片边框等 DOM 辅助层不得进入录像。
5. 屏幕裁剪 `screenCropNorm/#screenCropFrame` 与白板取景框 `recConfig.frame/#recFrame` 必须分离；`screenVideo` 只作隐藏取帧源，不能改成实时铺满预览。
6. MP4 优先使用浏览器原生 `MediaRecorder`；不支持时才懒加载 ffmpeg.wasm 转码，这是唯一允许的运行时 CDN 依赖。
7. 保持现有简单、方便的账号架构，不擅自增加复杂安全流程；但任何密钥和明文账号不得进入 Git。
8. 默认不用 subagent；先用 `rg` 定位，再小范围读取。只在用户明确要求或真正跨目录、可并行的开放探索时使用。
9. `whiteboard.html` 是创作者自用完整版：除登录、账号管理和用户权限控制外，所有白板、录制与导出功能都必须完整开放；对应核心功能需与 `whiteboard-pro.html` 同步。
10. 双语词典以 `locales/` 为唯一编辑源；修改 i18n 后必须运行 `node scripts/sync-whiteboard-i18n.mjs`，不得直接手改两个白板的 `WB_I18N_BUNDLE` 生成区，也不得让白板运行时依赖外部 locale 文件。

## 当前状态（2026-08-06）

- 两个白板版本的画布背景面板「米色」预设底色调整为 `#C6BAA7`（偏灰褐，更接近参考图），配套 `kraft` 牛皮纸纹理改为以白色散点/短纤维为主的纸质颗粒感，避免之前深色线条造成的横向条纹感；`paper` 书页纸纹保持淡雅效果不变。米色预设与纹理同步进入整份白板、当前幻灯片、撤销、文档保存、鸟瞰图、缩略图、笔迹播放和白板录像。
- 两个白板版本的系统日记模板（极简视频日记 / 三问复盘日记）改为直接应用到当前画布所选中的幻灯片，不再强制新建画布；若模板尺寸与当前幻灯片不一致会弹出双语确认对话框，确认后自动调整幻灯片尺寸并重排后续页面，取消则不应用。
- 两个白板版本新增本机幻灯片模板库：主菜单"新建"可选择空白画布、极简视频日记或三问复盘日记；两种日记分别为 9:16、16:9、3:4 独立编排，标题使用圆弧转角的左右开放黄色括号，英文日记标记位于括号底部连线高度；创建时自动填入当天日期并递增可编辑的 Day。当前幻灯片可保存为模板、插入现有画布或新建独立画布，并支持单模板/整库 JSON 导入导出；模板保存完整页内对象、背景、转场和元素操作栏，但不改变摄像头、提词器或录制配置。模板入口只在主菜单内，左上软盘和右侧新增幻灯片按钮保持原样。
- 两个白板版本的幻灯片内容动画浮动按钮已改用显式双语键组合当前类型、效果和自动播放状态；英文界面不再显示"文字 · 逐行"等中文动态标签，切换语言时按钮与提示同步刷新。
- 两个白板版本新增 Cmd/Ctrl+点击多选与智能批量属性面板：按住 Cmd/Ctrl 逐个点击对象可增减选择，按住 Cmd/Ctrl 拖框为累加框选；多选后属性面板按所选对象类型交集智能显示可批量编辑的控件——纯文字显示颜色/字体/字号/对齐，纯形状显示描边/填充/粗细/样式，混合类型显示共有颜色和透明度，分层按钮和透明度始终可用；改色/改透明度/字体等一键应用到全部选中对象，选择框顶部显示选中数量徽章。Cmd+点击空处保留当前多选不取消。多选支持整体移动、复制/粘贴、复制并粘贴、图层排序和批量删除。
- 两个白板版本的画布背景面板新增 `#2B7FD8` / `#14213D` / `#2CB1BC` / `#F28C5B` 四个预设色和内置书页纸张纹理；纸纹可应用于整份白板或当前幻灯片，并进入撤销、文档保存、鸟瞰图、缩略图、笔迹播放和录制画面。
- 两个白板版本新增录制友好的圆角矩形放大镜：点击或拖动可定位需要强调的文字、数字或图片局部，拖动四角可自由调整宽高，倍率可在 1.2×–5× 间连续调整；覆盖已缩小图片时会从原始位图高清重绘最上层图像，而不是二次放大画布像素。镜框进入白板录制，DOM 调整手柄不入镜，位置和显隐不写入白板文档。
- 两个白板版本在 idle 状态新增或切换幻灯片时，会检测目标页在当前缩放下是否超出安全视区；9:16 等高页面若在 100% 下放不进窗口会自动适应屏幕，已能完整显示的页面仍保留用户缩放和纵向位置。
- 两个白板版本的箭头头部会随箭身长度适度放大并使用更宽开口，同时保留短箭头的尺寸上限；干净、手绘和流动样式共享同一几何参数，长细箭头不再像末端小折线。
- 支持 Document Picture-in-Picture 的 Chrome/Edge 会先打开 760×590 置顶录屏裁剪器，每次新会话用 `preferInitialWindowPlacement` 和 `resizeTo()` 恢复大尺寸；窗口、标签页与整屏确认后自动显示 560×480 的"提词器＋暂停/停止"置顶窗。"收起提词器"会缩成 360×150 控制条并始终保留暂停/继续、停止和重新展开；整屏开始时只提示一次可能入镜，不做实时重叠检测。三类共享轨都尝试转移到主文档，防止原生关闭 PiP 后黑屏。
- 静态账号工具 `account-admin1.html` 的双语脚本标签结构已修复，页面 JavaScript 可完整执行，"读取现有 accounts.json"等账号管理按钮恢复事件绑定；语言切换器提前到初始化脚本前，首次加载即可正确绑定。
- 公共入口 `index.html` 的双语初始化、语言切换与登录状态检查已恢复为独立有效脚本，并补齐 `locales/index-init.js`；构建测试会同时校验入口内联脚本语法和本地脚本资源，避免发布后卡在"正在检查登录状态"。
- `whiteboard.html` 与 `whiteboard-pro.html` 已完成中英文双语：首次打开按浏览器首选语言选择中文或英文，主菜单可切换"跟随浏览器 / 中文 / English"并跨页面持久化；静态 DOM、动态菜单/提示、录制设置、贴纸和 Pro 专属界面统一由共享词典翻译，用户白板文字与提词器自定义内容不被改写。两个白板通过 `scripts/sync-whiteboard-i18n.mjs` 内嵌相同运行时与词典，仍保持独立单 HTML。
- 两个白板版本的左上主菜单已升级为本机多画布管理：旧单一自动存档会无损迁移为"画布-1"，新建与导入会保留现有画布并生成可切换卡片；可一键复制当前画布的全部幻灯片、对象、背景和讲稿并自动切换到副本，便于从中文版制作英文版；当前画布可独立导出或清空，每张卡片右上角提供带确认的删除按钮。
- 两个白板版本的新画笔已改用 Excalicord 同款 Perfect Freehand 核心：三档为细/粗/特粗（1/2/4，默认粗），固定使用 4.25 size 系数、0.6 thinning、0.5 smoothing/streamline 和正弦压力缓动；鼠标按点距渐进模拟压力，Apple Pencil 保存真实压力，首尾与急转圆角由一次闭合轮廓生成。v10 数据参与选择、擦除、缩放、复制和录制，旧固定笔画及实验性 v7 `f` 笔画保持可打开。
- 两个白板版本的左上角画布背景已从主菜单移为彩色调色盘直达按钮，点击即可打开颜色面板且角落色点实时显示当前有效底色；保存到文件与高频备份入口统一使用软盘图标，避免被误解为普通下载。
- 两个白板版本的右上媒体控制已改为紧凑双分段线框胶囊，摄像头与麦克风可独立启停并在录制中随时切换；摄像头状态同时决定页面头像和成品入镜，设置页不再维护重复开关。麦克风设备可预选、记忆和失败回退，开启时提供不回放到扬声器的实时音量条，动态静音不会中断系统声或录制时间轴。
- 刷新、自动恢复或打开文档后，右侧幻灯片尺寸与录制设置会从当前幻灯片的真实宽高恢复标准比例或 Custom；切换到历史上尺寸不同的页面时也保持三者一致，不再回落到默认 16:9。
- 两个白板版本的录制设置均提供 1080p、720p、480p 三档白板清晰度，首次默认 1080p 并记住本机选择；清晰度与幻灯片尺寸解耦且不常驻占用白板空间，录屏继续沿用共享源裁剪分辨率。白板录制用 `requestFrame()` 强制静止画面送帧，无摄像头/麦克风时用不可听但非零的 Web Audio 时钟持续写入 AAC 时间戳；原生 MP4 必须整段封装，避免 12 秒录制只显示约 5 秒。
- 两个白板版本的顶部控件会按真实尺寸自动避让：左上角只保留单色线框"主菜单"、画布背景和高频"保存备份"，主菜单展开本机画布管理面板；空间不足时再把形状、素材和清空收进紧凑绘画栏，极窄视口使用单行滚动兜底，iPad 触控按钮保持至少 44px。
- 两个白板版本的摄像头设置、页面头像、白板录制与录屏已统一使用自然美颜参数；页面头像改为实时效果画布，美颜以色度/亮度纹理分离来均匀肤色并保留毛孔与五官边缘，新增保护眼睛硬边缘的"黑眼圈/暗沉淡化"和只作用肤色的"红润肤色"，提亮使用保高光曲线而非白色雾面。
- 整屏录制裁剪界面支持全屏、16:9、4:3、3:4、9:16、1:1 与 Custom；默认跟随录制设置比例，非全屏预设复用 16:9 的上下安全边界避开菜单和 Dock，标准比例可等比缩放，Custom 可自由调整范围。
- 两个白板版本的提词器讲稿、速度和字号已纳入 v10 白板文档，可随 `.json` 文件和浏览器草稿保存、恢复；打开后回到未播放的编辑态，普通版与 Pro 版仍共用兜底缓存。
- 两个白板版本的提词器支持先选中文字再单独着色，一份讲稿可使用多种字色；局部颜色在编辑态与播放态同步，并随 v10 白板文档及浏览器草稿保存、恢复。
- 两个白板版本的提词器均可从右下角拖拽调整宽高，缩放与标题栏移动都会限制在当前浏览器视口内。
- 核心白板、对象变换、富文本、图片/贴纸、幻灯片、白板录制、录屏裁剪、摄像头效果与 MP4 回退已实现，真实媒体细节仍需真机持续验收。
- 商业化 MVP 已具备免费限制、购买弹窗、静态账号、Node + Neon 会话/设备授权、管理后台和公共静态构建。
- 静态账号与 Node/Neon 统一使用 `SHA-256(salt:usernameLowercase:password)` 和盐值 `wb-static-pro-salt-v1`；旧 Neon scrypt 账号在成功登录或静态登录审计时自动迁移，密码最少 4 位。
- 付费配置已拆为独立 `paywall.json`（59 元 / `leewen2017`），`accounts.json` 只保存账号；管理页可分别保存、拉取和部署两个文件。
- Neon 登录不再 `document.write()` 重载白板；当前页会由 `/api/session` 动态提升为 Pro，右下角显示用户名并提供统一退出。
- 公共入口会先检查静态会话，再以 5 秒上限检查 Neon 会话；有效会话直接替换跳转到白板，只有未登录、检查失败或超时时才显示登录页。
- 公共登录页的微信联系区支持复制当前 `paywall.json` 微信号，并可按需展开作者二维码；二维码默认收起，避免干扰已购用户登录。
- 已登录 Pro 用户的右下角常显"推荐给朋友"和账号菜单；联系/推荐弹窗可复制动态 `paywall.json` 微信号、扫描现有二维码、编辑并复制推荐语，支持可用时的系统分享；微信号复制使用紧凑图标，长账号可完整换行显示，首次点击不会自动改写剪贴板。
- 右下角"推荐给朋友"入口使用纯分享图标，悬停提示与无障碍标签保留完整按钮含义。
- Pro 模板的升级弹窗直接显示账号密码框，只保留"登录并打开 Pro"主操作和"继续使用免费版"次操作，不再用重复按钮展开登录表单。
- 三个用户登录入口（独立登录页、免费版升级弹窗、Pro 模板弹窗）均使用眼睛/斜线眼睛图标显示或隐藏明文密码，并保持悬停提示与无障碍状态同步。
- 男生、女生与综合彩铅贴纸新增 5 个"惊喜"表情：含同角色无道具版本、发现新事物版本及综合无道具版本，并同步到独立版与 Pro 模板。
- 男生与女生彩铅贴纸均新增"表扬"表情；综合组另加入用户提供的圆形"女孩点赞"贴纸，三张素材均同步到独立版与 Pro 模板。
- 新增"会议场景"彩铅漫画组，以 6 张连续贴纸表现远程会议、AI 实时记录、实时翻译、详细纪要、会议总结与行动计划，并同步到两个白板版本。
- 贴纸工具面板层级高于幻灯片边框、标签和"笔迹"按钮，画布辅助层不再穿透已打开的工具面板。
- 两个白板版本的选择工具均支持从空白处拖框多选，并可整体移动或按 Delete / Backspace 批量删除；单对象缩放、旋转和文字编辑保持不变。
- 两个白板版本在 iPad 和手机上支持画布双指捏合：两指中心同时控制缩放与平移，手势开始会撤销第一指产生的临时绘制、擦除或对象移动，松开一指后剩余触点不会误触发绘画。
- 两个白板版本选择画笔时会默认自动锁定以便连续作画；离开画笔只撤销自动锁定，用户通过锁按钮设置的手动锁定仍会保留。
- 提词器可从标题栏拖动且始终限制在视口内；打开或移动提词器不会再把右侧幻灯片选择面板移到其下方遮住。
- 两个白板版本的全部画布对象（含图片、贴纸、文字、图形和笔迹）均可置底、下移一层、上移一层或置顶；多选调整时保持组内相对顺序并只写一次撤销历史。
- 两个白板版本的单选和多选对象均支持 `Ctrl/Cmd+C` 后用 `Ctrl/Cmd+V` 粘贴，也可用 `Ctrl/Cmd+D` 直接复制并粘贴；副本保留样式、相对位置和图层顺序并可一次撤销，从外部复制的纯文字会直接粘贴成新的白板文字框。
- 两个白板版本的录制设置均新增可选文字水印：支持 40 字、九宫格定位、预览拖动、大小/透明度和本机记忆，并同时进入白板录制与录屏成品。
- 两个白板版本的右上录制操作已收进统一紧凑控制框；白板录制可在录制中显示/隐藏实时激光笔，录屏则按浏览器 cursor capability 尝试隐藏系统光标并在不支持时安全降级。
- 两个白板版本的录制控制框默认贴右上角，并在 1400px 以下自动下移一行；why：避免与居中的绘图工具框重叠。
- 两个白板版本均可从左上角设置整份白板底色，并允许当前幻灯片单独覆盖或恢复继承；颜色写入 v10 文档、撤销历史、鸟瞰图、笔迹播放和白板录像。
- 两个白板版本切换预设或自定义幻灯片比例时，会保持第 1 张中心并按列表顺序等距重排后续页面；页面内部对象随所属页面水平移动，页面外对象保持原位；之后新增或插入的页面继承当前相邻页面的实际宽高，避免比例相同但大小不同。
- 两个白板版本的幻灯片笔迹已提供彩铅铺色、水墨晕染、左上到右下铅笔描绘和"斜线推进"四种预设；四种均以约 4.4 秒先绘制黑色细墨线，再立即从头推进原色。原始文字、发丝和排线只走墨线通道，边缘检测不得在两侧重复描边；彩色色块不预填灰，线稿阶段占 34%，上色前段采用加速推进。分体按钮主区一键重播当前效果，箭头切换并记住本机选择，动画继续进入白板录制而预设弹层不入镜。
- 两个白板版本的幻灯片动画菜单新增"文字逐行"：当前页的独立文字框按从上到下、同高从左到右依次淡入并轻微上移，播放不改对象数据且会进入白板录制；形状内标签不参与。
- 两个白板版本均可逐页保存笔迹样式和"切入时自动播放"；正常切页会以笔迹第 0 帧作为转场终帧，转场结束后再开始内容揭示，主按钮仍可随时手动重播。
- 两个白板版本均支持为每张幻灯片设置无、淡化、推入或擦除进入转场、三档速度，以及无声音、翻书、轻柔滑动或柔和提示音与独立音量；编辑和正式录制切页会同步播放，声音混入成品音轨，全部设置写入 v10 文档并可一键应用到全部幻灯片。
- 两个白板版本的幻灯片动画菜单可把当前单选或多选对象加入逐页"元素操作栏"；只有绑定对象受显示控制，用户可任意点击命名按钮触发约 200ms canvas 淡入、单独显隐或全部显隐，并逐页统一选择无音效、轻点、柔和提示或轻柔滑动及音量。只有对象确实从隐藏变为显示时才播放，"全部显示"只响一次；录制中混入成品，暂停时仅本机播放。"笔迹"按钮保持紧贴幻灯片上方的原位置，元素操作栏固定在它正上方，项目过多时自动换行向上展开并全部显示，不使用横向滚动；操作栏不入镜。绑定、动作音效和稳定对象 ID 写入 v10 文档及撤销历史，当前显隐状态只保留在本次打开期间。
- `file://` 打开两个登录入口时会优先使用本机账号缓存，否则读取已部署的 `https://record.leewen.work/accounts.json`；Render 静态站开放该 JSON 只读跨域，账号 API 允许 `Origin: null` 作为 Neon 兜底。
- Neon 登录以及同时存在于 `accounts.json` 和 Neon 且密码一致的本地登录，会在用户放行后异步记录 IP、设备、浏览器和时间；管理页展示每个账号最近 100 条，并提示任意 1 小时窗口内的多 IP 情况。纯本地账号不记录。
- 账号管理页会合并 Neon 与 `accounts.json` 的全部账号；重叠账号只显示一次，静态独有账号标注来源并跳转到静态账号工具管理；列表按当前显示顺序展示连续 No. 序号。
- 账号管理页可将已启用的"Neon + 静态"账号转为纯静态账号，也可永久删除 Neon 独有账号；必须成功读取 `accounts.json` 并输入完整账号名确认，删除会级联清理设备和登录 IP 记录。
- 两个账号管理页的全部令牌和密码框均可通过眼睛按钮显示或隐藏内容；Neon 管理页提交或重开改密弹窗后会自动恢复隐藏。
- 两个白板版本的摄像头框均可按当前视口短边放大到接近全屏；触及窗口边界时自动回收位置，窗口缩小时同步收回，录屏合成不会越出成品画布。

## 下一步 TODO

- [ ] 部署后验证 `record.leewen.work` 的 `accounts.json` CORS、`auth.record.leewen.work` 的 `Origin: null`、Secure Cookie、`/health`、登录 IP 记录与 Render 环境变量。
- [ ] 真机验证 1/3 台设备限制、停用、改密、清空设备、刷新恢复和退出。
- [ ] 真机验证 Chrome/Edge 的置顶录屏裁剪器、关闭小窗后的 WhiteBoard 降级，以及 Safari 的白板页降级；并继续覆盖白板激光笔、录屏光标、整屏裁剪、摄像头/麦克风/系统声与 MP4 成品。

## 任务路由（只读对应小节）

| 任务 | 按需读取 |
|------|----------|
| 登录、账号、Neon、购买配置、发布白名单 | [`IMPL_NOTES · Auth`](docs/IMPL_NOTES.md#auth) |
| 对象、文字、图片、选择/缩放/旋转、绘图样式 | [`IMPL_NOTES · Objects`](docs/IMPL_NOTES.md#objects) |
| 幻灯片、比例、鸟瞰图、DOM 浮层 | [`IMPL_NOTES · Slides`](docs/IMPL_NOTES.md#slides) |
| 摄像头、麦克风、白板录制、录屏、导出 | [`IMPL_NOTES · Recording`](docs/IMPL_NOTES.md#recording) |
| 彩铅人物贴纸和图片资源 | [`IMPL_NOTES · Stickers`](docs/IMPL_NOTES.md#stickers) |
| 中英文、浏览器语言检测、手动切换、词典同步 | [`IMPL_NOTES · I18n`](docs/IMPL_NOTES.md#i18n) |
| 产品范围、里程碑、完整手测清单 | 按需读取 [`docs/PROJECT_PLAN.md`](docs/PROJECT_PLAN.md) |

## 文件地图

- `index.html` / `whiteboard-pro.html` — 公共入口与商业化白板模板；登录改动需成对检查。
- `whiteboard.html` — 独立白板应用；核心录制行为应与商业化模板保持一致。
- `accounts.json` / `paywall.json` / `account-admin1.html` — 静态哈希账号、独立购买配置与离线生成工具。
- `account-admin.html` / `server/` — Neon 管理页与 Node 账号 API。
- `scripts/build-static.mjs` / `tests/` / `render.yaml` — 发布白名单、自动测试与部署配置。
- `locales/` / `scripts/sync-whiteboard-i18n.mjs` — 双语运行时、词典、原文短语对照与两个单 HTML 的内嵌同步器。
- `docs/IMPL_NOTES.md` — 当前实现细节；`docs/PROJECT_PLAN.md` — 产品与里程碑；历史看 Git。

## 维护规则

- 每次收尾先看 `git diff --stat` 和目标文件窄 diff，再更新"当前状态 / 下一步 TODO"；已完成项立即删除或勾选。
- 小改只更新本索引；里程碑才更新 `PROJECT_PLAN.md`。实现陷阱写入 `IMPL_NOTES.md` 对应小节，不把长历史重新塞回本文。
- 文档开头保留"目的 / 目标读者 / 如何阅读"，结尾保留最多 3 条变更记录；更早历史交给 Git。
- 只有用户明确要求时才 commit/push；GitHub remote 必须使用 SSH。

## 变更记录

| 日期 | 变更内容 |
|------|----------|
| 2026-08-06 | 新增 Cmd/Ctrl+点击多选与智能批量属性面板（方案 A）；why：按住修饰键可逐个点选增减对象，属性面板按类型交集自动显示可批量编辑的控件，一键改色/字体/透明度应用到全部选中对象 |
| 2026-08-06 | 调整「米色」画布背景预设底色为 `#C6BAA7`，并把 `kraft` 纹理从深色线条改为白色散点/短纤维的纸质颗粒感；why：更贴近参考图的灰褐底色与白色点点纸纹 |
| 2026-08-06 | 系统日记模板从「新建画布」改为「应用到当前幻灯片」，并在尺寸冲突时询问是否调整当前幻灯片尺寸；why：减少重复创建画布，让用户在当前画布内直接套用版式 |
| 2026-08-04 | 新增多比例视频日记系统模板、本机幻灯片模板库及导入导出，并按参考图将标题装饰校正为圆弧开放括号、把英文标记嵌入底部连线；why：让每日录制可快速复用日期/Day 版式，同时保留原始视频日记的视觉识别 |
| 2026-08-02 | 修复幻灯片内容动画按钮的动态双语标签与语言切换刷新；why：避免英文界面残留"文字 · 逐行"等中文组合文本 |
| 2026-08-01 | 新增当前画布整份复制与自动避重命名；why：可保留中文版并直接编辑副本制作英文版 |
